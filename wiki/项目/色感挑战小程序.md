# 色感挑战小程序

> 微信小程序：色彩感知力测试 + 好友 PK。四种模式，段位系统，好友挑战裂变。

## 技术栈
- 微信小程序 + TypeScript
- 微信云开发（云函数 + 云数据库）
- Canvas 2D 分享卡

## 功能清单
- **色相排序**：将打乱色块按彩虹顺序排列
- **找不同色**：从网格中找出颜色差异的一块
- **颜色匹配**：从近似色中选出最接近目标色的选项
- **颜色记忆**：记忆颜色序列并复现
- **段位系统**：色感萌新 → 色彩学徒 → 色域行者 → 辨色大师 → 色感之神
- **排行榜**：今日排行 + 历史总榜
- **广告变现**：激励视频（复活/双倍）+ 插屏 + Banner
- **好友挑战**：生成挑战链接，好友扫码对战
- **新手引导**：每模式 3 步遮罩引导
- **分享卡**：Canvas 2D 生成分享图片

## 文件位置
- `C:\Users\20159\OneDrive\Desktop\项目\color-sense-miniapp\`
- AppID: `wxb7737bdb5a225b8d`
- 云环境: `cloud1-d7govbi2a61dbf07f`

## 启动方式
1. 微信开发者工具 → 导入项目 → 选择上述路径
2. 云函数右键上传部署
3. 云开发控制台创建数据库集合：users / daily_scores / game_records / challenges

## 架构
```
miniprogram/
├── pages/home/       # 首页 — 4 个模式卡片
├── pages/game/       # 游戏页 — 4 种模式共用
├── pages/result/     # 结果页 — 分数 + 段位 + 分享
├── pages/rank/       # 排行榜 — 今日/总榜
├── pages/challenge/  # 好友挑战 — VS 对战页
├── components/       # color-block / share-card / rank-item / tutorial-overlay / particle-burst
├── utils/            # color / score / ads / haptics / audio
├── styles/           # variables.wxss / animations.wxss
cloudfunctions/       # submitScore / getRanking / dailyReset / createChallenge / getChallenge
```

## 调试经验

### TypeScript 编译失败排查
- `btoa()` 不存在 → 手写 base64 编码替代
- `wx.arrayBufferToBase64` 不在类型定义中 → 有运行时 API 但 TS 编译不过
- 类型导入问题：`IAppOption` 必须 `export` 并在使用处 `import`
- 旧 `.js` 脚手架文件覆盖 TS 编译输出 → 删掉让工具重新编译

### CSS 变量失效
- WXSS `@import` 路径解析在不同版本开发者工具中不一致
- 解决：将所有 CSS 变量内联到 `app.wxss`，不使用 `@import`

### 全局防抖副作用
- 300ms `_tapGuard` 会拦截色块选择→槽位填入的连续快速操作
- 解决：只在需要防重复点击的模式（找不同/匹配/记忆）保留，色相排序去掉

### 字体 Emoji 渲染
- 微信小程序模拟器不完全支持 emoji 渲染
- 不影响真机，可直接忽略

## 待完成
- [ ] 每日签到系统（7 天日历 + 递增奖励）
- [ ] 成就系统（12+ 成就 + 成就墙）
- [ ] 游戏引擎抽取重构
- [ ] 性能优化（setData 批量合并、定时器降频）
- [ ] ICP 备案
